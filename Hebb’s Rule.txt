Hebb’s Rule is one of the earliest learning rules in Artificial Neural Networks proposed by Donald Hebb in 1949. It is based on the principle:

“Cells that fire together, wire together.”

This means that if two neurons are activated simultaneously, the connection (weight) between them is strengthened. Hebbian learning is an unsupervised learning rule because it does not require an external teacher to adjust weights.

